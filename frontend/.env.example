# URL of the backend Express/Socket.IO server.
VITE_BACKEND_URL=http://localhost:8888

# URL of the YouTube Music Desktop app's companion server (the port is
# user-configurable inside that app; this is its default).
VITE_YTMD_URL=http://localhost:9863
