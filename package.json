{
  "name": "listen2gether",
  "version": "1.0.0",
  "description": "Listen party using Spotify and YouTube Music",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
